buildscript {
    dependencies {
        classpath "com.android.tools.build:gradle:8.4.2"
    }
}

// IMPORTANT:
// Do NOT add repositories here.
// Repositories must be declared in settings.gradle (dependencyResolutionManagement).
